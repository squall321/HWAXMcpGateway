gateway_config.json
.venv/
__pycache__/
*.pyc
audit.jsonl
provision.env
gateway.log
gateway.pid
gateway_config.json.bak
gateway_config.json.bak.*

# data-migrate 가 남기는 옛 경로 원본(유예 뒤 사람이 지운다). 미추적이면 deploy-all 의 git stash -u 가 치운다
*.pre-move-*
